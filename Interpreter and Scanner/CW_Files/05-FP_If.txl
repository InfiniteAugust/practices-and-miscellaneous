let x = 6.98 in
  let z = 3.14 in
    let s = 2.1e-2 in
      if (x * s < z) || (1.6 == x)
      then
        if \( s > z ) && (1.6 < s)  ! note that \ is logical negation
	then sin( x)
	else cos( exp( x * z) )
      else exp( cos( x + z))
		     
